import { client } from '../lib/appwrite';

export async function getDevices(userId: string) {
  // TODO: Fetch devices for user from backend
  return [];
}

export async function addDevice(userId: string, device: any) {
  // TODO: Add device in backend
  return null;
}

export async function removeDevice(userId: string, deviceId: string) {
  // TODO: Remove device in backend
  return null;
}