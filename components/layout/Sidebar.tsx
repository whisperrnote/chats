export default function Sidebar() {
  return (
    <aside style={{ width: 72, background: 'var(--color-surface)', borderRight: '1px solid var(--color-border)' }}>
      {/* TODO: Add navigation icons, user avatar, etc. */}
      <div style={{ padding: 16, textAlign: 'center' }}>Sidebar</div>
    </aside>
  );
}
