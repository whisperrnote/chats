export default function Topbar() {
  return (
    <header style={{ height: 56, background: 'var(--color-surface)', borderBottom: '1px solid var(--color-border)', display: 'flex', alignItems: 'center', padding: '0 16px' }}>
      {/* TODO: Add search, actions, profile, etc. */}
      <span>Topbar</span>
    </header>
  );
}
