export default function ProfileForm() {
  return (
    <section style={{ padding: 24 }}>
      <h2>Profile</h2>
      {/* TODO: Avatar, display name, bio, status, etc. */}
      <div>ProfileForm</div>
    </section>
  );
}
