export default function CallPanel() {
  return (
    <section style={{ width: 400, background: 'var(--color-surface)', borderLeft: '1px solid var(--color-border)', height: '100vh', padding: 24 }}>
      {/* TODO: Call controls, video/audio streams, participants */}
      <div>CallPanel</div>
    </section>
  );
}
