export default function SecondaryPanel() {
  return (
    <aside style={{ width: 280, background: 'var(--color-surface)', borderLeft: '1px solid var(--color-border)', height: '100vh', padding: 16 }}>
      {/* TODO: Contextual info, chat members, search, etc. */}
      <div>SecondaryPanel</div>
    </aside>
  );
}
