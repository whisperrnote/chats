export default function SettingsPanel() {
  return (
    <section style={{ width: 360, background: 'var(--color-surface)', borderLeft: '1px solid var(--color-border)', height: '100vh', padding: 24 }}>
      {/* TODO: Theme, animation, profile, security, recovery, extensions */}
      <div>SettingsPanel</div>
    </section>
  );
}
