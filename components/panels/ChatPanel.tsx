export default function ChatPanel() {
  return (
    <section style={{ flex: 1, background: 'var(--color-bg)', display: 'flex', flexDirection: 'column' }}>
      {/* TODO: ChatHeader, MessageList, MessageInput, reactions, etc. */}
      <div>ChatPanel</div>
    </section>
  );
}
